// import * as path from 'path';
// import _ from 'lodash';
// import { exportData } from '../utils';

export async function exportPokedex(target: string) {
  console.log('- pokedex');

  // TODO export pokedex

  // if (result.length) {
  //   console.log(`\twriting ${result.length} pokedexes...`);
  //   await exportData(path.join(target, `gen${gen.num}`, 'pokedex.json'), result);
  // }
}
